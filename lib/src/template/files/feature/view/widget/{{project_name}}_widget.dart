import 'package:flutter/material.dart';

class {{project_name_capitalize}}Widget extends StatelessWidget {
  const {{project_name_capitalize}}Widget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      '{{project_name}}',
    );
  }
}
